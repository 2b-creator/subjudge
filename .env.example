# Database Configuration
# 根据你使用的数据库类型选择对应的连接字符串

# PostgreSQL (推荐，支持 JSON 字段)
DATABASE_URL=postgres://username:password@localhost/subjudge

# MySQL
# DATABASE_URL=mysql://username:password@localhost/subjudge

# SQLite (用于开发测试)
# DATABASE_URL=sqlite://subjudge.db?mode=rwc
